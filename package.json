{
  "name": "neon-salvage-static-beta",
  "version": "6.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check game.js && node --check src/balance.js && node --check src/save.js && node --check src/diagnostics.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
