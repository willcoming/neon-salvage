{
  "name": "neon-salvage-static-beta",
  "version": "7.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check phaser-game.js && node --check game.js && node --check src/balance.js && node --check src/save.js && node --check src/diagnostics.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "phaser": "3.90.0"
  }
}
